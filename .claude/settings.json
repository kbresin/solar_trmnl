{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) python3 -B -m py_compile \"$f\";; *.sh) bash -n \"$f\" && shfmt -w \"$f\";; esac; } 2>&1 || true"
          }
        ]
      }
    ]
  }
}
